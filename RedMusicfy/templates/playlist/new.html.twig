{% extends 'base.html.twig' %}

{% block title %}Nueva Playlist{% endblock %}

{% block body %}
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <div class="card">
                    <div class="card-body">
                        <h1 class="card-title text-center mb-4">Crear nueva Playlist</h1>
                        {{ include('playlist/_form.html.twig', {'button_label': 'Crear'}) }}
                        <div class="text-center mt-3">
                            <a href="{{ path('app_playlist_index') }}" class="btn btn-danger">Volver a la lista</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}